using System;

namespace HashParser
{
    class Program
    {
        static void Main(string[] args)
        {
            var input = ""

            var t = new ParamFile();
            t.Open(inputFile);
        }
    }
}

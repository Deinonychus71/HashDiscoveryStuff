<Window x:Class="HashRelationalResearch.GUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:HashRelationalResearch.GUI.Controls"
        xmlns:vm="clr-namespace:HashRelationalResearch.GUI.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" MinHeight="1050" Height="1050" MinWidth="1800" Width="1800">
    <Window.DataContext>
        <vm:MainWindowVM />
    </Window.DataContext>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0">
            <MenuItem Header="_File">
                <MenuItem Header="_Open" Click="MenuItem_Click" />
                <Separator />
                <MenuItem Header="_Exit" />
            </MenuItem>
        </Menu>
        <controls:ResearchTab Grid.Row="1" Margin="0,0,-507,-354"></controls:ResearchTab>
    </Grid>
    
    
</Window>
